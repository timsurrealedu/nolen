# Normal-operations fixture

This deterministic, offline fixture provides benign SSH sessions, standard processes, and routine `/etc/passwd` reads. It is safe to execute and makes no network or system calls.

It is an initial baseline, not a complete training dataset. Additional scenarios must be added before ML training.
