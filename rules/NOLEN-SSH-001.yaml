id: NOLEN-SSH-001
name: SSH Brute Force
severity: high
match:
  event.category: authentication
  event.action: login
  event.result: failure
  service.name: ssh
group_by: [source.ip, user.name, host.id]
condition: { count: 10, within: 60s }
mitre: { tactic: credential-access, technique: T1110 }
